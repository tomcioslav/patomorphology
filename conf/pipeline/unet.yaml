# Pipeline = (which LightningModule + DataLoader factory + per-pipeline
# run params). The architecture is the `net:` group; the cache is the
# `dataset:` group; the LR schedule is the `lr:` group.
name: unet

batch_size: 4
num_workers: 4
precision: 32-true
