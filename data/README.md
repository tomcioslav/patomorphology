# Data

Place raw and processed datasets in this directory. Contents are gitignored — only this README and `.gitkeep` are tracked.

Suggested layout:

```
data/
├── raw/         # untouched source images / annotations
├── interim/     # cleaned but not feature-engineered
└── processed/   # ready for model training
```
